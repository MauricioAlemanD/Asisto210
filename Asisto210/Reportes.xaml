<Page x:Class="Asisto210.Reportes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Asisto210"
      mc:Ignorable="d" 
      d:DesignHeight="531" d:DesignWidth="1083" Background="Transparent"
      Title="Reportes">

    <Page.Resources>
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border
                  x:Name="Border" 
                  Grid.ColumnSpan="2"
                  CornerRadius="0"
                  Background="Black"
                  BorderBrush="Black"
                  BorderThickness="1" />
                <Border 
                  Grid.Column="0"
                  CornerRadius="0" 
                  Margin="1" 
                  Background="White" 
                  BorderBrush="Black"
                  BorderThickness="0,0,1,0" />
                <Path 
                  x:Name="Arrow"
                  Grid.Column="1"     
                  Fill="White"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
                />
            </Grid>
            <!--<ControlTemplate.Triggers>
            <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#808080" />
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#E0E0E0" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                <Setter TargetName="Border" Property="BorderBrush" Value="#AAAAAA" />
                <Setter Property="Foreground" Value="#888888"/>
                <Setter TargetName="Arrow" Property="Fill" Value="#888888" />
            </Trigger>
        </ControlTemplate.Triggers>-->
        </ControlTemplate>

        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="{x:Type TextBox}">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>

        <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton 
                            Name="ToggleButton" 
                            Template="{StaticResource ComboBoxToggleButton}" 
                            Grid.Column="2" 
                            Focusable="false"
                            IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                            ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                            Margin="3,3,23,3"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox"
                            Style="{x:Null}" 
                            Template="{StaticResource ComboBoxTextBox}" 
                            HorizontalAlignment="Left" 
                            VerticalAlignment="Center" 
                            Margin="3,3,23,3"
                            Focusable="True" 
                            Background="Black"
                            Foreground="Black"
                            Visibility="Hidden"
                            IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup 
                            Name="Popup"
                            Placement="Bottom"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            AllowsTransparency="True" 
                            Focusable="False"
                            PopupAnimation="Slide">

                                <Grid Name="DropDown"
                              SnapsToDevicePixels="True"                
                              MinWidth="{TemplateBinding ActualWidth}"
                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border 
                                x:Name="DropDownBorder"
                                Background="White"

                                BorderThickness="1"
                                BorderBrush="Black" CornerRadius="10,10,10,10"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                            <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="true">
                                <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="0"/>
                                <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEditable"  Value="true">
                                <Setter Property="IsTabStop" Value="false"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
            </Style.Triggers>
        </Style>

        <!-- SimpleStyles: ComboBoxItem -->
        <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border Name="Border"
                              Padding="2"
                              SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#FFB5B5B5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>


    <Grid x:Name="mainGrid">
        <Label x:Name="lblReporte" Content="Reporte" Margin="18,10,772,492" FontFamily="Poppins" FontSize="18"/>

        <Border x:Name="dockerReporte" BorderThickness="1" Margin="18,40,283,10" Background="White">
            <Border.Effect>
                <DropShadowEffect Opacity="0.1"/>
            </Border.Effect>
            <Grid x:Name="dockReporte">
                <DataGrid x:Name="dvgReporte" AutoGenerateColumns="False" Foreground="Black"  FontSize="10" BorderBrush="White" AlternatingRowBackground="White" RowBackground="White" HorizontalGridLinesBrush="White" VerticalGridLinesBrush="White" CanUserResizeColumns="False" SelectionMode="Single" RowDetailsVisibilityMode="Collapsed" EnableRowVirtualization="False" IsSynchronizedWithCurrentItem="False" >
                    <DataGrid.Resources>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Width" Value="100"/>
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="{x:Type DataGridRowHeader}">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Background" Value="White"/>
                        </Style>
                        <Style TargetType="{x:Type DataGridRow}">
                            <Setter Property="IsEnabled" Value="False"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Background>
                        <SolidColorBrush Color="White" Opacity="100"/>
                    </DataGrid.Background>

                </DataGrid>
            </Grid>
        </Border>
        <Label x:Name="lblReporte_Edicion" Content="Generar reporte" Margin="820,10,-30,492" FontFamily="Poppins" FontSize="18"/>
        <Border x:Name="dockerGenerarReporte" BorderThickness="1" Margin="820,40,0,0" Background="White" HorizontalAlignment="Left" Width="253" Height="481" VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect Opacity="0.1"/>
            </Border.Effect>
            <Grid x:Name="dockGeneararReporte">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="14*"/>
                    <ColumnDefinition Width="237*"/>
                </Grid.ColumnDefinitions>
                <Label x:Name="lblFiltrosGeneracion" Content="Filtros de generacón" HorizontalAlignment="Left" Margin="7,10,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontWeight="DemiBold" FontSize="16" RenderTransformOrigin="0.39,0.114" Width="176" Grid.ColumnSpan="2"/>
                <CheckBox x:Name="cmbSomo" Content="Directivos" HorizontalAlignment="Left" Margin="3,83,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <ComboBox x:Name="cmbRol_Editar" Text="ID" HorizontalAlignment="Left" Margin="0,279,0,0" VerticalAlignment="Top" Width="230" Height="20" Background="White" BorderBrush="Black" BorderThickness="1" Grid.Column="1">
                    <ComboBoxItem Content="Diario"/>
                    <ComboBoxItem Content="Semanal"/>
                    <ComboBoxItem Content="Mensual"/>
                </ComboBox>
                <Label x:Name="lblFiltrosGeneracion_Copiar" Content="Mostrar" HorizontalAlignment="Left" Margin="0,52,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" RenderTransformOrigin="0.39,0.114" Width="176" Grid.Column="1"/>
                <CheckBox x:Name="cmbSomo_Copiar" Content="Administradores" HorizontalAlignment="Left" Margin="106,83,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <CheckBox x:Name="cmbSomo_Copiar1" Content="Docentes" HorizontalAlignment="Left" Margin="3,104,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <CheckBox x:Name="cmbSomo_Copiar3" Content="Empleados" HorizontalAlignment="Left" Margin="106,104,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <Label x:Name="lblFiltrosGeneracion_Copiar1" Content="Turno" HorizontalAlignment="Left" Margin="0,125,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" RenderTransformOrigin="0.39,0.114" Width="176" Grid.Column="1"/>
                <CheckBox x:Name="cmbSomo_Copiar2" Content="Matutino" HorizontalAlignment="Left" Margin="3,157,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <CheckBox x:Name="cmbSomo_Copiar4" Content="Vespertino" HorizontalAlignment="Left" Margin="106,157,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <Label x:Name="lblFiltrosGeneracion_Copiar2" Content="Asistencia" HorizontalAlignment="Left" Margin="3,178,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" RenderTransformOrigin="0.39,0.114" Width="176" Grid.Column="1"/>
                <CheckBox x:Name="cmbSomo_Copiar5" Content="Resgistrada" HorizontalAlignment="Left" Margin="0,210,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin" Checked="cmbSomo_Copiar5_Checked"/>
                <CheckBox x:Name="cmbSomo_Copiar6" Content="No registrada" HorizontalAlignment="Left" Margin="106,210,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <Label x:Name="lblFiltrosGeneracion_Copiar3" Content="Periodo de tiempo" HorizontalAlignment="Left" Margin="3,252,0,0" VerticalAlignment="Top" FontFamily="Poppins" FontSize="14" RenderTransformOrigin="0.39,0.114" Width="176" Grid.Column="1"/>
                <CheckBox x:Name="cmbSomo_Copiar7" Content="Justificada" HorizontalAlignment="Left" VerticalAlignment="Center" FontFamily="Poppins" FontSize="14" Grid.Column="1" FontWeight="Thin"/>
                <Border x:Name="bbtnElditarPersonal" BorderBrush="#FF4E1417" BorderThickness="1" Margin="6,398,0,0" Height="21" VerticalAlignment="Top" HorizontalAlignment="Left" Width="240" Grid.ColumnSpan="2">
                    <Label x:Name="btnEliminarPersonal1" Content="Genearar reporte" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25" Width="229" HorizontalContentAlignment="Center" Foreground="#FF4E1417" Cursor="Hand"/>
                </Border>
                <Border x:Name="bbtnElditarPersonal_Copiar" BorderBrush="#FF4E1417" BorderThickness="1" Margin="6,434,0,0" Height="21" VerticalAlignment="Top" HorizontalAlignment="Left" Width="240" Grid.ColumnSpan="2" Background="#FFFFC7C7">
                    <Label x:Name="btnEliminarPersonal2" Content="Guardar reporte" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25" Width="229" HorizontalContentAlignment="Center" Foreground="#FF4E1417" Cursor="Hand"/>
                </Border>
            </Grid>
        </Border>

    </Grid>
</Page>
